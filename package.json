{
  "name": "agent-bittrees-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Source-grounded agent contribution portal for agent.bittrees.org.",
  "scripts": {
    "check": "node --check src/server.mjs && node --check src/portal.mjs && node --check scripts/build.mjs && node --check scripts/serve-dist.mjs && node --check scripts/smoke-check.mjs && node --check test/portal.test.mjs",
    "build": "node scripts/build.mjs",
    "dev": "node --watch src/server.mjs",
    "smoke": "node scripts/smoke-check.mjs",
    "start": "node src/server.mjs",
    "start:dist": "node scripts/serve-dist.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
